// import Decimal from "decimal.js-light";

export class DecimalOperations {

//     plus( v1: number| string, v2: number| string ): string{
//         return new Decimal(v1).plus(v2).toString();
//     }

//     minus( v1: number | string, v2: number| string ) : string {
//         return new Decimal(v1).minus(v2).toString();
//     }

//     multiply(v1: number | string, v2: number | string): string {
//         return new Decimal(v1).times(v2).toString();
//     }

//     divide(v1: number | string, v2: number | string): string {
//         return new Decimal(v1).dividedBy(v2).toString();
//     }

//     subtract(v1: number | string, v2: number | string): string {
//         return new Decimal(v1).minus(v2).toString();
//     }

//     greaterThan(v1: number | string, v2: number | string) : boolean {
//         return new Decimal(v1).greaterThan(v2);
//     }

//     greaterThanOrEquals(v1: number | string, v2: number | string): boolean {
//         return new Decimal(v1).greaterThanOrEqualTo(v2);
//     }

//     lessThan(v1: number | string, v2: number | string): boolean {
//         return new Decimal(v1).lessThan(new Decimal(v2));
//     }

//     lessThanOrEquals(v1: number | string, v2: number | string): boolean {
//         return new Decimal(v1).lessThanOrEqualTo(new Decimal(v2));
//     }

//     toDecimalPlaces(v1: number | string , value: number){
//         return new Decimal(v1).toDecimalPlaces(value).toString();
//     }
}